{
  "name": "everymusic-web-client",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "begin": "vite --port 3000 --host",
    "dev": "npm run begin",
    "start": "npm run begin",
    "compile": "vite build",
    "build": "npm run compile",
    "serve": "vite preview",
    "preview": "npm run serve",
    "check": "echo 'Frontend ready'",
    "desktop": "tauri dev",
    "desktop:build": "tauri build",
    "android": "tauri android dev",
    "android:build": "tauri android build",
    "ios": "tauri ios dev",
    "ios:build": "tauri ios build"
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/cli": "^2.11.2",
    "@vitejs/plugin-react": "^6.0.2",
    "vite": "^8.0.14"
  }
}
