const BootScreen = () => (
  <div className="boot-screen" role="status">
    <p className="brand-mark">EVERY.MUSIC</p>
    <span className="sr-only">Starting Every.music</span>
  </div>
);

export default BootScreen;
